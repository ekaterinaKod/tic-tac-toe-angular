import { ShowPlayerSymbolPipe } from './show-player-symbol.pipe';

describe('ShowPlayerSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPlayerSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
